# Meilisearch Configuration
MEILISEARCH_URL=http://localhost:7700
MEILISEARCH_ADMIN_API_KEY=your_admin_api_key_here

# Client-side environment variables (accessible in the browser)
NEXT_PUBLIC_MEILISEARCH_URL=http://localhost:7700
NEXT_PUBLIC_MEILISEARCH_READ_API_KEY=
NEXT_PUBLIC_DEFAULT_INDEX=your_default_index_name

# Note: The admin API key is required for the /api/meilisearch/indexes endpoint
# to fetch available indexes. This should have admin or master key permissions.
# For search operations in the frontend, you can use a read-only API key. Pinning requires to GET